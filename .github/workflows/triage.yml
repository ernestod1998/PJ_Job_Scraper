name: Manual Job Triage

on:
  workflow_dispatch:

concurrency:
  group: job-scraper-commit-push
  cancel-in-progress: false

jobs:
  triage:
    name: Score new roles vs candidate profile
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Anthropic SDK
        run: pip install anthropic

      # Profile/resume arrive as env vars straight from Actions secrets —
      # never written to disk, never committed (the repo is public).
      # --limit bounds worst-case daily spend; raise/lower as the cost knob.
      - name: Run triage agent
        env:
          # Gemini is the cheap ranking backend (triage_agent picks it first).
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          CANDIDATE_PROFILE: ${{ secrets.CANDIDATE_PROFILE }}
          CANDIDATE_RESUME: ${{ secrets.CANDIDATE_RESUME }}
        # --judge audits each fit score >= 50 (the shortlist) and writes judge_*
        # fields the dashboard renders as ✅/⚖️ markers. Advisory, never gates.
        run: python triage_agent.py --limit 300 --judge

      - name: Commit scores to repo
        run: |
          git config user.name  "ernestod1998"
          git config user.email "61074783+ernestod1998@users.noreply.github.com"
          python ci_commit_push.py \
            --message "chore: update fit scores [$(date -u '+%Y-%m-%d %H:%MZ')]" \
            scores.json
